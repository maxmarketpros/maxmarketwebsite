[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Give the SEO audit endpoint enough time for two PageSpeed Insights calls (~25s typical).
[functions."seo-audit"]
  timeout = 60
